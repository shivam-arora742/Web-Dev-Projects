//string:
const str='hello world'

// The slice() method extracts parts of a string and returns the extracted parts in a new string.
let res=str.slice(0,5)
console.log(res)

//to extract from end of string:
let end=str.slice(-1)
console.log(end)