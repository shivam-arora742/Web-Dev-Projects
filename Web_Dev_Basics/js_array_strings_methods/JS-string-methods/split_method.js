//string:
const str='hello world'

//The split() method is used to split a string into an array of substrings, and returns the new array.

//when no parameters:consider string as 1 elemnt of array.
let res=str.split()
console.log(res)

//for parameter(""):splits every character.
res=str.split("")
console.log(res)